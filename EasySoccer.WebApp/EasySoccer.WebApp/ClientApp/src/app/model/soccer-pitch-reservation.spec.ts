import { SoccerPitchReservation } from './soccer-pitch-reservation';

describe('SoccerPitchReservation', () => {
  it('should create an instance', () => {
    expect(new SoccerPitchReservation()).toBeTruthy();
  });
});
